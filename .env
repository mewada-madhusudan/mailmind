# ── Azure AD / Microsoft ────────────────────────────────────────────────────
# Create an App Registration at https://portal.azure.com → Azure Active Directory → App Registrations
# Redirect URI: http://localhost:5173 (for dev) / your production URL (for prod)
# Enable "Access tokens" and "ID tokens" under Authentication
# Required API Permissions: Microsoft Graph → Mail.Read, Mail.ReadWrite, MailboxSettings.Read, User.Read

VITE_AZURE_CLIENT_ID=your-azure-app-client-id-here
VITE_AZURE_TENANT_ID=common     # use 'common' for multi-tenant or your tenant ID for single org

# ── LLMSuite ────────────────────────────────────────────────────────────────
# Your LLMSuite instance base URL (OpenAI-compatible /v1 endpoint)
VITE_LLMSUITE_BASE_URL=https://your-llmsuite-instance.com/v1
VITE_LLMSUITE_API_KEY=your-llmsuite-api-key-here
VITE_LLMSUITE_MODEL=gpt-4o    # or whichever model is available in your LLMSuite deployment
